CREATE KEYSPACE IF NOT EXISTS mio WITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 1 };
DESC KEYSPACE mio;
use mio;
CREATE TABLE IF NOT EXISTS results (prod text, average double, PRIMARY KEY (prod) );



